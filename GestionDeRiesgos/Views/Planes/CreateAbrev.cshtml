@model GestionDeRiesgos.Models.AbrevPlanes

@{
    ViewData["Title"] = "CreateAbrev";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>CreateAbrev</h1>

<h4>Abreviciones de Planes</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CreateAbrev">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="abreviacion" class="control-label"></label>
                <input asp-for="abreviacion" class="form-control" />
                <span asp-validation-for="abreviacion" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="categoria" class="control-label"></label>
                <input asp-for="categoria" class="form-control" />
                <span asp-validation-for="categoria" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
