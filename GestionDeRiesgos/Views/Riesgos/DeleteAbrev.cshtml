@model GestionDeRiesgos.Models.AbrevRiesgos

@{
    ViewData["Title"] = "DeleteAbrev";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>DeleteAbrev</h1>

<h3>Are you sure you want to delete this?</h3>
@if (TempData["MensajeDelete"] != null)
{
    //Si tiene datos mostramos el alert en caso de que falle la autenticacion
    <div class="alert alert-warning" role="alert">
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"> </button>
        <stromg>
            @TempData["MensajeDelete"]
        </stromg>
    </div>
}
<div>
    <h4>AbrevRiesgos</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.abreviacion)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.abreviacion)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.categoria)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.categoria)
        </dd>
    </dl>
    
    <form asp-action="DeleteAbrev">
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
